# Production site origin for canonical URLs and Open Graph (omit on localhost).
# VITE_SITE_URL=https://raghavkalyan.com
#
# Never put secrets, API keys, or private endpoints in VITE_* — they are embedded in the client bundle.
